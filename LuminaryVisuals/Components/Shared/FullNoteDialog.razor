@inject CloudflareR2Service cloudflareR2Service
<MudDialog>
    <DialogContent>
        <MudHtmlEditor Html="@Text" Label="Note:" HtmlChanged="OnNoteTextChanged" OnFileUpload="cloudflareR2Service.UploadFileAsync">
            <MudHtmlToolbarOptions />
        </MudHtmlEditor>

    </DialogContent>
    <DialogActions>
        <MudButton  OnClick="@Cancel" Color="Color.Error" Variant="Variant.Outlined" Size="Size.Large">Close</MudButton>
        <MudButton Disabled="@isDelayed" OnClick="@Save" Color="Color.Info" Variant="Variant.Outlined" Size="Size.Large">Save</MudButton>
    </DialogActions>
</MudDialog>

@code{
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; }
    [Parameter] public string Text { get; set; } = "";
    [Parameter] public EventCallback<string> NoteTextChanged { get; set; }
    private bool success;
    private MudForm form;
    private DateTime _minDate = DateTime.Now.Date;
    private bool isDelayed = true;
    protected override async Task OnInitializedAsync()
    {
        isDelayed = false;
        StateHasChanged();
    }
    private void OnNoteTextChanged(string text)
    {
        Text = text;
        NoteTextChanged.InvokeAsync(text);
    }
    private void Save()
    {
        MudDialog.Close(DialogResult.Ok(Text));
    }
    private void Cancel()
    {
        MudDialog.Cancel();
    }

}
<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.h6">
        @((MarkupString)Message)
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Error" Variant="Variant.Outlined" Size="Size.Large" OnClick="Cancel">No</MudButton>
        <MudButton Color="Color.Success" Variant="Variant.Outlined" Size="Size.Large" OnClick="Submit">Yes</MudButton>
    </DialogActions>
</MudDialog>
    
@code {
        [CascadingParameter]
        private IMudDialogInstance MudDialog { get; set; }
        [Parameter] public string Message { get; set; }

        private void Submit() => MudDialog.Close(DialogResult.Ok(true));

        private void Cancel() => MudDialog.Close(DialogResult.Cancel());
}

